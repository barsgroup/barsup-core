language: python
python:
  - "3.3"
  - "3.4"
env:
  - BUP_TESTS=$TRAVIS_BUILD_DIR/src/barsup/tests
  - CC=gcc
install:
  - easy_install -U pip
  - pip install -r REQUIREMENTS.test


# command to run tests
script:
  - py.test --pep8 --cov .

after_success:
  - coveralls